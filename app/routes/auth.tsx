export const  meta= () => ([
    {title: "ResAI | Auth"},
    {name: 'description', content: 'Description'}
])

const Auth = () => {
    return (
        <div>
            Auth
        </div>
    );
}

export default Auth;
